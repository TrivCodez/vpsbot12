[project]
name = "vps-bot"
version = "1.1.0"
description = "Discord VPS manager bot"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4.0",
    "docker>=7.1.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
